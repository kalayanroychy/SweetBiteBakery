# Local connection to VPS database via SSH Tunnel
# 1. Run connect-db.bat
# 2. Copy the credentials from the remote .env output
# 3. Replace the placeholder below

# Format: postgres://user:password@localhost:5432/database_name
DATABASE_URL=postgres://replace_user:replace_password@localhost:5432/replace_db_name

# Session Secret (should match remote or be consistent)
SESSION_SECRET=sweetbite-bakery-secret

# Environment
NODE_ENV=development
